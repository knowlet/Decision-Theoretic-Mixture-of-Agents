# 實際執行的 OpenJev 對抗評測

本節在模型池替換之外，加入**新執行的地端神經模型推論**。使用獨立專案 `TheoLeeCJ/openjev`，固定 commit `b4782a6c953f05c6255706d7a219f4e032af5b58`，不是 TypeSafe 專有 Jev。OpenJev 讀取最後位置的選項 token logits，再正規化成機率，不解碼自由文字。原版 `direct.score` 以 Git blob `943f34728d1966bf2325d6e6d34d2a7043cd6a56` 核對；模型載入明確改為 CPU，而非假裝原版 CUDA-only loader 原封不動執行。後端保留 BF16 權重與各層輸出，CPU Linear／Conv1d 使用 FP32 累加。它與 native BF16 CPU 非逐位元相同：自製相容性題目的選擇不變，但一個 logit 差 0.125。這只支持執行可行性，不代表全域數值誤差保證，也不是原作者 GPU 跑分的重現。

## 比較相同任務，不拼接不同榜單

沿用前述四個二元 ProEval 資料集與替換後工作模型池。在原切分內，以固定 salted hash 選取不重複群組：每資料集 32 題測試、16 題開發，共 128 題測試與 64 題開發。不依難度、正確性、分歧或 confidence 選題。這是舊資料的子集，**不是額外增加的 128 個獨立 benchmark 題目**。協議在新選擇器跑分前提交，但已看過 v1.3 舊結果及自製 runtime probes；不是外部預註冊。

**相同候選批次比較**讓 empirical fixed-three、panel majority 與 OpenJev 都取得同一組三個答案，panel 以訓練／開發流程選出。OpenJev 另取得題目文字與經驗式終端規則可用的訓練條件錯誤率；這不是当前題目的 gold。經驗式方法不作題目語意推理，表示法與歸納偏差仍不同。提供校準估計避免把充分校準的本方法拿去對比毫無背景的神經選擇器，但不能使雙方预訓練歷史相同。

**完整策略比較**允許 Bellman 等方法提早停止、減少查詢。它比較終端損失與資源，不是純終端選擇器消融。模型池與上限相同，但固定三答案的 OpenJev 每次付出三次工作模型成本。任何一方均不能取得當前 gold、未查詢答案或完美驗證器；輸出只能來自已取得 panel 或保留判斷，不接受新生成答案。

## 受測演算法與新推論

主要模型是固定 Qwen3.5-4B，對應所選 upstream 支援的家族；Qwen3.5-0.8B 為明確的小模型容量對照，不宣稱重現 upstream 的 4B 排名。兩者使用相同 readout、選題、狀態與選項排序。權重不訓練、不量化；過長輸入直接失敗，不截斷。Hosted CPU 數字不與原作者 GPU 或專有服務延遲混比。

`OpenJev raw` 在三個回傳動作與 defer 中選最高分。`OpenJev calibrated` 先選最高分的非 defer 候選，使用 64 題開發資料擬合正則化 logistic success predictor，特徵是候選 log odds 與正規化動作熵。開發標籤若全同類，使用 Beta(1,1) 平滑常數。測試成功率估計至少 0.75 才回傳，否則按保留判斷損失 0.25 轉交。測試標籤不參與校準；此開發 split 也供既有策略選參，不是另一個獨立評估集。

每模型執行 192 次主要 forward、32 次反向選項測試與 8 次相同輸入重跑。4B 分八個 worker、0.8B 分兩個，各 worker 記一次不納入延遲的 warmup：**4B 共 240 次、0.8B 共 234 次，完成 benchmark 合計 @@TOTAL_FORWARDS@@ 次**。更早的自製探針與被取代的 native CPU 嘗試不計為已完成評測證據。工作模型仍為歸檔，專有 Jev API、CERA 訓練、新工作模型生成與辯論皆為零。原始和校準版本共用 readout，不暗中再問一次模型。

## 結果必須搭配 coverage 與查詢數

目標為錯答損失 1 或保留判斷損失 0.25，加上每份工作模型答案 0.01。它**沒有把 CPU 秒數憑空換成美元**。星號準確率只計有作答案例，必須與 coverage 同讀。

@@SCORE_TABLE@@

沒有「本方法必須勝出才發布」的條件。較低點估計不等於優越性證明。指定對比在相同 128 群組配對，於各資料集內重抽並等權平均；區間條件於已擬合策略、calibrator 與已記錄輸出，不涵蓋重訓不確定性。

@@PAIRS@@

另提供 98.75% percentile 區間，對四個事前指定宏平均對比作保守的 Bonferroni 式多重比較調整。小樣本 bootstrap 覆蓋率仍只是近似；探索性資料集及校準比較不因此提升成確認性結論。

@@FAMILY@@

## 信心、順序效應與實際計算

@@CALIBRATION@@

Raw score 是候選動作的選項質量，與候選正確性比較只能視為診斷 proxy，不代表它原本就估計該事件。開發校準版本明確估計正確性，但少量資料不保證普遍可靠。型別合法仍可答錯，較高 option mass 也不是較低風險的證書。

主要題目選項位置以固定規則打散；反转四選項時，題目、合法動作與標籤不變。探針量測選擇翻轉及重新對應選項後的 total variation 距離。每模型 32 題不增加新獨立題目；反轉及重跑都在原題相同 worker 上，避免探針混入主機變更因素。

@@ROBUSTNESS@@

@@TIMING@@

神經時間包含 tokenization 與 forward，不含下載／載入、warmup、calibrator 擬合及 artifact 寫入。經驗式時間是 Python 重播／帳本平均開銷，不是硬體匹配的神經服務延遲。校準版本額外 success predictor 未獨立計時，顯示時間是完整路徑的下界 proxy。各 shard CPU 型號已記錄；這些數字不與 Jev 服務或 OpenJev 的 GPU／JSON 生成加速比混用。

主要分數未加入 selector 計算成本，對神經選擇器相對有利。獨立敏感度表對每次神經選擇加上 0、0.001、0.005、0.01、0.02、0.05、0.1 的假設損失費用；它們不是觀測價格。部署評估仍需真實工作模型延遲、平行度、帳單與算力效用權重。錯誤率算術不是更快的語言模型，神經表示能力也不是免費。

## 理論補充：已生成卻未被使用的回答

**命題四：未使用輸出的無關性。** 假設在生成前只依初始問題與路由分數選定最終回應代理；其他代理不影響該選擇、被選者 prompt、潛在回答分布、環境或未來決策。若其輸出不被讀取，且額外啟動成本嚴格為正，則刪除那些啟動保持最終回答分布不變；原本會啟動額外代理時，預期成本嚴格下降。

證明是把兩系統配對到相同問題、路由分數及被保留代理的亂數。依不變性前提，最終輸出相同，額外輸出不在終端決策的依賴路徑上，因而不改變損失；扣除正成本即得結論。前提排除綜合、根據生成內容選人、有用副作用與訓練介入。

這說明 CERA-MoA 的開放題推論規則——選最高 familiarity 代理——必須與其訓練貢獻分開，不代表共同演化訓練或所有多代理推論皆多餘。本次 OpenJev 會讀取多個答案，不符合未使用輸出的前提。此命題是依賴關係論證，不是新通用 ensemble 定理。

## 證據界線與發布追蹤

實驗來源 commit 為 `@@EXPERIMENT_COMMIT@@`，執行紀錄為 @@EXPERIMENT_RUN@@。後續報告 commit 另記，不偽裝成重新跑过的推論來源。

這次是實際對抗評測，不只有 API contract；但受限於 128 題、四種既有基準、單一歸檔、單一神經模型家族、假設成本及無新工作模型生成。它不建立 SOTA、專有 Jev 品質、CERA 訓練效果或個人偏好辨識。更強研究需跨模型家族及獨立收集資料，以因子設計分離表示法、終端選擇與查詢策略，並固定授權與資源合約。

上游歸屬：TheoLeeCJ，*OpenJev*，MIT 程式授權及另行揭露的模型條款，固定 revision 如上。https://github.com/TheoLeeCJ/openjev。Checkpoint 權利另計；本產物不散布模型權重或字型。
