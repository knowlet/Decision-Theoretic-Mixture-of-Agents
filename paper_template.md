---
title: "不是更多模型，而是更好的停止規則"
subtitle: "Decision-Theoretic Mixture-of-Agents：分歧、驗證、偏好澄清與有限預算最優控制的可重現模擬研究"
author: "Decision-Theoretic Mixture-of-Agents 專案 · AI 輔助技術報告"
date: "2026 年 9 月 17 日 · v1.1.0 · 修訂版"
lang: zh-TW
---

# 摘要 {-}

多模型協作（Mixture-of-Agents, MoA）能產生多種候選答案，卻不自動決定何時值得取得更多資訊、如何處理使用者偏好，以及何時應停止。本研究檢驗一個先前提出、但尚未經實驗支持的流程：風險路由、異質模型 fanout、分歧偵測、事實驗證或偏好澄清，再進行綜合或保留判斷。我們將其明確化為啟發式策略 P0，與三個對照組比較：依任務類型在校準集選擇的單一模型 C1、固定三模型融合 C2，以及單步資訊價值級聯 C3；另實作可选择平行查詢、序列查詢與停止的 Bellman 策略 P1。

研究使用五類合成決策、六個機率觀測通道、共同失誤與個別偏好不可辨識機制。20 次獨立重複包含 500,000 次校準抽樣及 400,000 個留出測試案例。主目標同時計入錯誤／轉人工損失、正規化成本及批次關鍵路徑延遲。P0 的平均目標值為 **0.6148**，優於 C1 的 **0.9535** 與 C2 的 **1.0076**，卻劣於 C3 的 **0.3824**。估計版 P1 為 **0.3752**，比 P0 改善 **39.0%**，但相對 C3 僅改善 **1.9%**。主分布下，移除相關性建模的估計版平均值略優於 P1，差異不顯著，故結果不支持所有原先構想的元件都必要。

我們證明：在已知聯合分布、固定損失與成本、有限行動集合及預算的條件下，Bellman 策略為該策略空間內的 Bayes 最優；並導出分布估計誤差下的 regret 上界。這不是任意真實 LLM 任務的全域最優定理。凍結策略在驗證器退化、偏好訊號退化及成本漂移下會失去優勢；探索性實驗更顯示，驗證器是否必須先取得候選答案，足以逆轉 P0 與 C3 的相對排名。本研究結論是：**原流程沒有被證明最優；能獲得條件式最優保證的是明確的決策問題與求解策略，不是元件堆疊。**

**關鍵詞：** Mixture-of-Agents；value of information；metareasoning；selective prediction；Bayesian decision theory；preference elicitation；optimal stopping。

修訂另增加資訊來源匹配的非自適應基線：其 $J=0.3817$，與 P1 的差距為 $0.00653$；這使「主要收益來自新資訊，而不是複雜規劃」的解釋更可信。完整學習曲線、路由錯誤與驗證器共同失誤分析均標示為事後探索性，沒有追溯宣稱預註冊。公開發行為 GitHub 技術報告，不是期刊／會議接受或 DOI 登錄。

## English abstract {-}

We evaluate a risk-routed, disagreement-aware Mixture-of-Agents workflow using a finite-support simulator with correlated errors, private preferences, verification, and costly deferral. Twenty independent runs contain 500,000 calibration draws and 400,000 held-out cases. The original heuristic P0 beats single-worker and fixed-panel controls but loses to a myopic value-of-information cascade. A fitted Bellman policy P1 reduces P0's composite loss by 39.0%, but improves on the cascade by only 1.9%. Removing correlation modeling does not significantly worsen performance. We prove Bayes optimality only within the specified finite decision problem and derive a distribution-estimation regret bound. Verifier degradation, cost shifts, and candidate-before-verification constraints change policy rankings. This is a reproducible synthetic falsification study, not a real-model benchmark or a universal optimality claim. No language-model endpoint was called.

\clearpage
\tableofcontents
\clearpage

# 研究問題、證據範圍與主張修正

## 從架構建議變成可否證假說

本研究的出發點是一個開放式問題：當多個模型對設計、命名、工程取捨或其他沒有唯一答案的問題產生分歧時，系統應如何產生最終決策？先前提出的方案將風險路由、異質顧問、分歧分析、驗證、偏好處理和停止機制串在一起。這是一個合理的設計方向，但「功能較完整」並不推出「品質較好」「成本較低」或「數學最優」。

我們因此將研究問題拆為三個可檢验命題。第一，原流程的明確實作 P0 是否比三個較簡單的對照組降低決策損失？第二，若把策略選擇形式化為有限預算下的序列決策，完整前瞻 P1 相對單步級聯 C3 的增益是否足以支持額外複雜度？第三，優勢能否在相關失誤、資訊品質改變與工具依賴條件下維持？

本研究不以找出支持 P0 的設定為成功標準。P0 輸給對照組、某個消融組沒有變差，以及排名在不同條件下翻轉，均是必須保留的主要發現。

## 這次實際執行了什麼

本次確實執行 Python 程式、Monte Carlo 抽樣、有限狀態動態規劃、完整支撐集期望值計算與單元測試。測試集不是人工填入的成績表：每個案例都有任務類型、隱藏答案與六個預先抽出的潛在觀測；策略只能逐次讀取已支付成本的通道。不同策略共用相同案例，因此可做配對比較。

本次**沒有**呼叫 OpenRouter Fusion、Hermes MoA、Sakana Fugu，或任何其他 LLM API；沒有訓練大型語言模型；沒有測量真實 token、美元帳單、網路延遲或自然語言答案品質。本文的「模型」是受控的 Bernoulli 觀測通道，並非真實品牌模型的替身。相關產品文件只用來說明架構背景，不能被視為被測產品實作。[5–7]

這是一篇完整的**合成決策實驗論文／研究報告**，不是產品跑分論文，也尚未經外部同儕審查。Bellman 原理與資訊價值概念並不是本研究首創；本文的工作是把它們落實為可稽核、能暴露原提案弱點的實驗。[10]

# 相關工作

## 固定聚合、顧問模式與動態協調

Together 的 MoA 研究採用分層結構：各層模型讀取前一層輸出，將其作為生成答案的輔助資訊。[1] 這提供了多模型融合的清楚基線，但是否增加下一次推論，本身仍是另一個控制問題。

OpenRouter 的 Fusion 文件描述平行 panel、分析者及外層模型：分析者回傳共識、矛盾、覆蓋缺口等結構化比較，外層模型再回答；模型也可自行決定是否呼叫 Fusion。[5] Hermes 文件則將 aggregator 定義為實際發出回答與 tool call 的 acting model，reference models 提供顧問資訊，並可調整 fanout cadence。[6] Sakana 的 Conductor 工作採用經強化學習訓練的協調模型。[7] 這三者分別突顯分歧分析、代理迴圈整合及學習式協調，但文件並未替本文的目標函數提供普遍最優證明。

## 模型多樣性不等於品質提升

Self-MoA 的研究指出，混入不同模型有時會降低候選品質；模型內的多次生成也可能比跨模型混合更好。[2] 因此，「品牌不同」「訓練來源不同」或「錯誤相關性低」只能是選模訊號，不能單獨充當最優準則。選模仍須同時考慮個別準確度、條件互補性、成本、延遲以及當前問題的損失。

## 路由、選擇性回答與推理資源配置

FrugalGPT 探討成本限制下的 LLM 級聯，[3] RouteLLM 則研究利用偏好資料進行模型路由。[4] 它們提示我們：合理對照組應包含成本感知級聯，而不僅是粗糙的多數決。SelectiveNet 代表將拒答／保留判斷納入模型目標的 selective prediction 路線；因此只報回答正確率、不報 coverage，可能產生誤導。[9]

Hay 等人的 metalevel MDP 直接形式化計算選擇。[11] Metareasoning 將「是否再花計算資源」視為決策本身，最優系統配置會隨環境變異與推理成本改變。[10] 本研究採用這一視角，把額外詢問模型、查證、澄清偏好及停止都視為可比較的行動。Adaptive submodularity 能在特定結構條件下支持貪婪策略的近似保證，[12] 但本文沒有證明自己的目標具有那些條件；XOR 互補性反例更說明任意資訊價值問題不具備自動的貪婪保證。

LLM-as-a-judge 研究另指出位置、冗長度與自我偏好等偏誤。[8] 為避免本次架構比較受到文字評審偏誤污染，我們直接以生成分布的隱藏狀態評估決策損失。代價是：本研究因此**沒有檢驗語言綜合與自然語言評審的能力**。

# 問題形式化

## 狀態、觀測與行動

每個任務具有可觀察類型 $T=t$，以及隱藏狀態 $Y\in\{0,1\}$。在事實任務中，$Y$ 是正確選項；在偏好任務中，$Y$ 是在該使用者效用函數下較好的選項，而不是普遍的「客觀真理」。

通道集合為 $\mathcal M=\{A,B,C,D,V,E\}$。$A$ 到 $D$ 代表候選工作模型，$V$ 代表事實查證／grounded-answer 通道，$E$ 代表向決策者取得偏好資訊。每個通道回傳 $O_i\in\{0,1\}$。我們直接建模聯合分布

$$
P_t(Y,O_A,O_B,O_C,O_D,O_V,O_E), \tag{1}
$$

而不假定給定 $Y$ 後所有工作模型彼此獨立。歷史 $h$ 僅含已查詢通道與其輸出。策略可以停止並輸出 $a\in\{0,1,\bot\}$，也可查詢尚未使用的非空批次 $S\subseteq\mathcal M$；$\bot$ 表示有成本的轉人工／保留判斷。每次最多累計取得四個通道，不允許重複查詢同一通道。

完整的潛在輸出向量只供模擬器保存與評估，策略不能讀取尚未取得的元素。這是 common-random-numbers 配對設計，不是讓策略偷看其他模型答案。

## 損失、成本與延遲

主目標是最小化

$$
J_P(\pi)=\mathbb E_P\!\left[
\ell_T(a,Y)+\lambda C(\pi)+\mu L(\pi)
\right]. \tag{2}
$$

終端損失為

$$
\ell_t(a,Y)=
\begin{cases}
w_t\,\mathbf 1\{a\ne Y\},&a\in\{0,1\},\\
d,&a=\bot.
\end{cases} \tag{3}
$$

主設定 $d=1.5$、$\lambda=0.1$、$\mu=0.04$。批次成本與延遲分別是

$$
c(S)=\sum_{i\in S}c_i,
\qquad
\tau(S)=\max_{i\in S}\tau_i. \tag{4}
$$

序列批次的延遲再相加，因此平行三模型不是三倍延遲。本文所有 $c_i$ 與 $\tau_i$ 都是**人工指定的正規化單位**，不對應金額、token 數或秒。$E$ 的成本與延遲代表偏好澄清的負擔；$d$ 則代表轉人工的預期總損失，並不是假設轉人工免費或一定正確。

終端選擇由各策略共同的 Bayes 決策器產生：令 $p_h=P(Y=1\mid h,t)$，則

$$
R_t(h)=\min\{w_t p_h,\;w_t(1-p_h),\;d\}. \tag{5}
$$

所有主策略使用同一組估計分布與同一個終端規則。對照組不是多數決，也沒有被指定成較弱的文字評審。

## 本研究所稱的「最優」

最優只對指定的 $P$、$\ell$、$\lambda$、$\mu$、行動可行性及預算有意義。本文區分：**已知分布的最優策略 $\pi_P^*$**、**由校準資料估計分布後求得的策略 $\pi_{\widehat P}^*$**，以及真實未知環境的表現。前者可以被定理刻畫；第二者受估計误差影響；第三者還受模型更新、工具依賴、文字生成與觀測偏差等因素影響。三者不能互換。

# 方法與對照組

## 五個主比較策略

@@POLICY_TABLE@@

C1 **分別針對每一個任務類型**，在該類校準分布上，從 $A$ 到 $D$ 選出使式 (2) 最小的一個工作模型；不是事後拿測試集找最佳模型，也不是純粹挑準確率最高者。C2 也**依任務類型**，從四個工作模型的所有三模型子集合中，選擇該類校準分布上目標最小的 panel；同一類內的候選集合不隨觀測改變，所有題目都查三個，再做完整聯合後驗聚合。v1.0 將這個行為寫成全域固定選擇，與程式不符；v1.1 修正文字，沒有因此更動原始數值。由於 C1 和 C2 的結構只使用工作模型，它們不會自行取得 $V$ 或 $E$；這是架構限制，不能把跨組差距全部解釋為「融合演算法」的效果。

C3、P0 與 P1 可取得相同六個通道、使用相同四次 acquisition 上限，且共享終端規則。各組不必花完預算；強迫所有方法花同樣多錢會抹去停止策略的價值。本文因此同時報成本、終端損失、延遲與敏感度，而不是將共同預算上限誤稱為「相同實際花費」。

## P0：將原構想明確化

原提案沒有完整閾值與政策，故本研究選擇一個可重現的具體版本，不宣稱它是所有可能實作的代表。低利害偏好任務直接採用預定選項；簡單事實任務只查 $A$；其他任務先平行取得異質 panel $\{A,C,D\}$。事實任務若三者不同意，才查 $V$；高利害偏好任務則再查 $E$。最後以共同 Bayes 規則輸出或轉人工。

這個設定其實對原構想相當寬容：它假設已經正確辨識任務類型，且「偏好處理」能真正向使用者取得新資訊，而不是僅由 LLM 列出評估面向。**Criteria extraction 並不等於 preference elicitation**；只列出「成本、外觀、維護」不會自動得到使用者權重。

P0 的弱點假說也很明確：它可能為已經有直接查證途徑的問題支付多餘 panel 成本，或因模型一致而錯誤地跳過驗證。

## C3：單步資訊價值級聯

C3 在每個歷史比較停止與單一通道的期望查詢後風險：

$$
Q^{(1)}(i\mid h)=\lambda c_i+\mu\tau_i+
\mathbb E\left[R_t(h\cup\{(i,O_i)\})\mid h\right]. \tag{6}
$$

若沒有通道使風險降低到足以抵銷成本，就停止；否則查詢最佳通道並重新計算。它不是固定順序的弱級聯，而是根據完整估計聯合分布選擇單步最有價值資訊的強對照。補充組 C3-batch 還允許一次前瞻所有可行批次，減少「P1 只因可平行而勝出」的疑慮。

## P1：有限前瞻的決策控制器

P1 的工作不是永遠開啟 panel，而是在所有可行行動中選擇：直接停止、取得單一通道、平行取得多個通道，或先觀測後再決定。只要是相同限制下的 C1、C2、C3 或 P0 行為，都包含在其政策搜尋空間中。

在六個二元通道下，歷史有 $3^6=729$ 種編碼：每個通道分別是未觀測、0 或 1。四次 acquisition 上限下共有

$$
\sum_{k=0}^{4}\binom{6}{k}2^k=473 \tag{7}
$$

個可行歷史。程式先計算各歷史的後驗質量，再由剩餘預算最少的狀態向前回推。這是一個小型精確求解器，不是可直接擴張到任意長自然語言對話的通用演算法。

# 數學結果與完整證明

## 命題一：強制增加模型不存在普遍嚴格優勢

**敘述。** 在允許任意任務分布、且額外呼叫具有正成本的問題集合上，一個強制多於一次查詢的方案，不可能普遍嚴格優於可只查一個模型的方案。

**證明。** 建構 $Y$ 為公平二元變數，模型 $A$ 完全正確，呼叫成本在目標單位下為 $0.1$，其餘模型只能提供同一答案的冗餘副本。單查 $A$ 的錯誤損失為零、總損失為 $0.1$；強制查三個模型同樣零錯誤，但總損失 $0.3$。因此後者嚴格較差，普遍嚴格優勢命題不成立。此證明只否定強制額外計算的普遍優越性，**沒有否定可自適應退化為單模型的政策空間**。$\square$

## 命題二：沒有偏好資訊，模型共識不能識別使用者效用

**敘述。** 若 $Y\sim\operatorname{Bernoulli}(1/2)$ 表示使用者較偏好的選項，且所有非澄清輸出 $O$ 與 $Y$ 獨立，則任何只讀取 $O$ 的自動決策規則，其無條件選對機率至多為 $1/2$；增加此類模型數量不改变結論。

**證明。** 獨立性給出 $P(Y=1\mid O=o)=1/2$。對任意決定 $a(o)\in\{0,1\}$，條件選對機率為 $1/2$；再對 $O$ 取期望仍為 $1/2$。隨機化的決策是確定性規則的混合，結果相同。其資訊論表達為 $I(Y;O)=0$。轉人工可改变最終服務流程，但不能被算成自動決策辨識出 $Y$。$\square$

這是自行車棚問題的核心邊界：多個模型對顏色達成共識，與知道特定使用者的效用函數，是不同的問題。

## 命題三：有限決策問題的 Bayes 最優性

令 $r$ 為剩餘 acquisition 預算，$\mathcal A(h,r)$ 是在歷史 $h$ 中合法、非空、未使用且大小不超過 $r$ 的批次集合。定義

$$
V_t(h,0)=R_t(h), \tag{8}
$$

以及

$$
\begin{aligned}
Q_t(S\mid h,r)
&=\lambda c(S)+\mu\tau(S)\\
&\quad+\sum_z P_t(O_S=z\mid h)
V_t(h\cup\{(S,z)\},r-|S|),\\
V_t(h,r)&=\min\left\{R_t(h),\;
\min_{S\in\mathcal A(h,r)} Q_t(S\mid h,r)\right\}.
\end{aligned} \tag{9}
$$

**前提。** 聯合分布、損失與行動成本已知；每個潛在觀測在回合開始時由指定分布產生；查詢不會改變尚未觀測訊號的生成機制；行動與終端選項有限；可行性規則明確；延遲按照式 (4) 計算；預算有限。

**結論。** 每一步選擇式 (9) 最小值的策略，達到所有滿足這些限制之觀測式策略的最小期望損失。

**證明。** 對剩餘預算 $r$ 做歸納。當 $r=0$，策略只能做終端選擇，式 (5) 就是逐一比較三個選項的最小風險，因此式 (8) 最優。假設所有小於 $r$ 的剩餘預算都成立。任意可行策略在 $(h,r)$ 的第一步，只可能是停止，或選某一合法批次 $S$。停止的最低風險為 $R_t(h)$。若選 $S$，必先付出 $\lambda c(S)+\mu\tau(S)$；對每個可能輸出 $z$，根據歸納假設，後續策略的期望損失不可能低於 $V_t(h\cup\{(S,z)\},r-|S|)$。對 $z$ 加權後，就是式 (9) 中該 $S$ 的下界。選擇所有第一步下界之最小值，並在每個子問題接續最優策略，可以達到此下界。故式 (9) 為全體可行策略的最小值。隨機化第一步只會形成這些值的凸組合，不會更小。$\square$

**重要限定。** 這是標準 Bellman 原理在本文決策問題上的推導，不是新發明的通用最優定理。它證明的是 $\pi_P^*$，不是未經校準的 LLM 自述信心，也不是估計版 P1 在所有測試集上一定擊敗其他估計器。

## 推論：正確停止規則不是「意見已一致」

令式 (9) 中採取批次 $S$ 的值為 $Q_t(S\mid h,r)$。最優停止的充要判斷是

$$
R_t(h)\le \min_{S\in\mathcal A(h,r)}Q_t(S\mid h,r). \tag{10}
$$

一致或分歧只是影响條件分布的觀測，不是停止條件本身。即使三個工作模型全數同意，只要它們可能共同失誤，而且查證的預期損失改善大於成本，仍應繼續。反之，若錯誤代價很小，即使三個模型完全不同意，仍可能應該立即停止。

在低利害偏好任務，未取得資訊時風險為 $0.05/2=0.025$。任何非空查詢的主設定目標成本至少為 $0.081$，已經超過全部可消除的終端風險，所以立即停止就是最優，而不必舉行模型辯論。

## 命題四：分布估計誤差的 regret 上界

對固定任務類型，假設任何可行策略在每個完整潛在結果上的總損失都介於 $0$ 與 $M_t$；採用總變異距離定義

$$
\operatorname{TV}(P,\widehat P)=\tfrac12\sum_\omega|P(\omega)-\widehat P(\omega)|\le\epsilon_t.
$$

則估計分布上的最優策略滿足

$$
J_P(\pi_{\widehat P}^*)-J_P(\pi_P^*)\le 2M_t\epsilon_t. \tag{11}
$$

**證明。** 由損失有界，對任意固定策略 $\pi$，有 $|J_P(\pi)-J_{\widehat P}(\pi)|\le M_t\epsilon_t$。將 regret 拆解為

$$
\begin{aligned}
&J_P(\pi_{\widehat P}^*)-J_P(\pi_P^*)\\
={}&[J_P(\pi_{\widehat P}^*)-J_{\widehat P}(\pi_{\widehat P}^*)]\\
&+[J_{\widehat P}(\pi_{\widehat P}^*)-J_{\widehat P}(\pi_P^*)]\\
&+[J_{\widehat P}(\pi_P^*)-J_P(\pi_P^*)].
\end{aligned}
$$

第一與第三項各不超過 $M_t\epsilon_t$；第二項因 $\pi_{\widehat P}^*$ 在 $\widehat P$ 上最優而不大於零，得式 (11)。$\square$

本文使用安全但鬆的界：$M_t=\max(w_t,d)+\lambda$ 乘以四個最高通道成本之和，再加 $\mu$ 乘以四個最高延遲之和。模擬中能直接計算真實 TV；真實部署通常不知道 $P$，因此不能將這個可計算性直接搬到生產環境。本界也不表示「模型自報 95% 信心」就有 95% 保證。

## 為什麼單步資訊價值仍可能不足

建構兩個公平獨立位元 $O_1,O_2$，並令 $Y=O_1\oplus O_2$。任一單獨位元對 $Y$ 都沒有資訊；兩者合起來卻完全決定 $Y$。若每次查詢成本為 $0.12$，不查詢的錯誤風險是 $0.5$，單查一次後停止為 $0.62$，查兩次則僅需 $0.24$。單通道單步策略會停止，但允許聯合前瞻者不會。

這個精確反例證明 C3 並非一般最優；卻不證明 P1 的完整前瞻在每個應用都值得。額外改善必須大於真實控制器的規劃、估計與維護成本，而這些成本尚未完整納入主模擬。

# 實驗設計

## 任務分布與效用模型

@@TASK_TABLE@@

任務類型在主實驗中被直接提供給所有策略，沒有額外語意分類誤差。這是一項刻意簡化且偏樂觀的前提，不代表實際 router 能完美識別事實與價值衝突。各類 $Y$ 的先驗均為 $1/2$。

偏好任務可具體理解為兩方案 $x_0=(1,0)$ 與 $x_1=(0,1)$。使用者權重在 $(0.8,0.2)$ 與 $(0.2,0.8)$ 間切換，較佳方案由 $Y$ 表示；$w_t$ 是將效用差轉換為任務利害後的錯選損失。這只模擬二選一的私人偏好，不涵蓋完整多目標 Pareto frontier 或社會選擇問題。

## 相關失誤生成機制

事實題具有全域錯誤位元 $G$，以及只影響 $A,B$ 的同家族錯誤位元 $F$。工作模型輸出為

$$
O_i=Y\oplus G\oplus\big(F\mathbf1\{i\in\{A,B\}\}\big)\oplus\varepsilon_i, \tag{12}
$$

其中個別翻轉 $\varepsilon_i$ 相互獨立；$P(\varepsilon_i=0)=r_i$。因此 $r_i$ 是相對於潛在翻轉目標的個別正確機率，**不是工作模型對真實答案的總準確率**。

@@GENERATOR_TABLE@@

主設定中 $V$ 與上述共同失誤獨立，對 $Y$ 的正確率為 0.96；事實任務的 $E$ 完全無資訊。偏好任務中，$A$ 至 $D$ 追隨共同群體偏好 $Z\sim\operatorname{Bernoulli}(0.7)$，各自跟隨 $Z$ 的機率為 0.92、0.90、0.85、0.80，但 $Z$ 與個別使用者 $Y$ 獨立。偏好任務的 $V$ 無資訊，$E$ 對使用者偏好的觀測正確率為 0.95。

**關鍵建模限制：** 主設定的 $V$ 是能直接給出二元答案的 grounded-answer 通道，而不是只能對既有候選做是／否判定的驗證器。真實程式測試、證明檢查與部分工具呼叫常需要先有候選；後文另以探索性可行性限制檢查此假設，不把它混進事前主分析。

## 資源參數

@@RESOURCE_TABLE@@

共同 finalizer 的成本被省略，策略規劃與機率估計的真實 CPU 成本也沒有併入 $J$。若生產版需要昂貴的額外 LLM judge、embedding、分類或上下文整理，其成本不能照此省略。本文的相對延遲只來自設定的批次關鍵路徑。

## 校準、留出測試與防洩漏

每次重複對每一類取得 5,000 次校準抽樣，估計 128 個聯合結果格子的機率；每格加入 0.5 的 Dirichlet pseudocount。共 20 次獨立重複，相當於 500,000 次校準抽樣。程式以 multinomial sufficient counts 產生及保存校準分布，並非將 500,000 段自然語言文本當作訓練資料。

每次重複另由獨立亂數流抽取 20,000 個案例，合计 400,000 個留出測試案例。亂數種子由 `(260917, replicate, stream)` 派生；校準使用 stream 0、測試使用 stream 1。所有策略在每次重複使用同一個 $\widehat P$ 與同一批測試案例。測試標籤只在決策結束後評分，沒有被用來調整策略或閾值。

我們在首次執行前保存 `protocol.json`，但**沒有進行外部公開預註冊**。其雜湊用於重現，不是外部時間戳證明。驗證器依賴與漂移後更新分布的分析是在主結果產生後新增，故明確標成探索性。

## 統計分析與數值驗證

主指標是式 (2)。另報終端決策損失、平均成本、延遲、acquisition 數、coverage 及 selective accuracy。coverage 是不轉人工的比例；selective accuracy 只在已回答案例上計算，不能單獨作為總品質指標。

信賴區間以 **20 個獨立重複**為推論單位，使用 $t_{19}$ 的雙尾 95% 區間；主差異使用同一重複內的配對損失，再針對四個事前指定比較採 Holm 多重檢定校正。沒有把同題經過不同策略的結果當成獨立樣本。消融比較屬診斷性分析，不混充新的事前主假說。

每類只有 128 個完整潛在結果，五類合計 640 個支撐點。因此除了抽樣，我們也能對固定策略精確積分真實期望值。精確積分用於驗證模擬、評估分布漂移，以及確認已知分布 P1 的下界地位；它不是額外的真實 LLM 測試集。

# 主實驗結果

## 原提案並非最佳主策略

@@MAIN_TABLE@@

表中 $J$ 越低越好。P0 相對 C1 與 C2 的確改善，但成本感知 C3 已大幅超越它。P1 相對 P0 降低 **38.98%** 的總目標與 **41.29%** 的正規化 acquisition 成本。相對 C3，P1 的目標降低 **1.90%**，成本反而增加 **1.36%**，而模型化延遲降低 **7.92%**。這是品質、成本、平行化與選擇性回答的取捨，不是所有軸向都更好。

若將 P0 到 P1 的總改善當成 100%，C3 已取得其中 **96.97%**。因此，在這個分布上，「先做可靠的單步資訊價值控制」比「一開始就搭完整多層 MoA」更值得優先驗證。這是實驗設定內的設計推論，不是對所有生產場景的建議排名。

![五個主策略的配對實驗結果。誤差棒為跨 20 次重複的 95% 信賴區間；單位為人工指定的合成目標，不是自然語言 benchmark score。](figures/primary_objective.png){width=85%}

## 配對比較

@@PAIR_TABLE@@

差值定義為表中前者減後者；正值表示前者更差。P0 輸給 C3 的差異遠大於 P1 與 C3 的差距。統計顯著不能代替工程意義：本實驗的樣本與低維結構使很小差異也能被估計，但並沒有量測實作 P1 所需的工程維護成本。

## 依任務類型拆解

@@CATEGORY_TABLE@@

一般困難事實題中，C3 與 P1 通常直接使用 $V$；P0 則先取得三個模型的輸出，因此浪費成本，且在一致但錯誤時可能省略驗證。高利害偏好題中，C3 與 P1 直接取得 $E$，P0 先取得的三個群體觀點不能識別個人效用，只增加負擔。低利害偏好題中，三種自適應策略都不取得額外資訊，接受小額不確定性。

高風險事實題是完整前瞻的主要受益區域：P1 在已知分布下的初始行動為平行查詢 $\{D,V\}$，再依輸出決定後續；C3 初始只查 $V$。對高利害任務，便宜且有條件互補性的工作模型仍可能值得取得，但不表示需要固定三模型 panel。

## 一致不代表安全

对 P0 的 $\{A,C,D\}$ panel，困難事實題下三者一致的機率為 **45.19%**；在已經一致的條件下，三者共同答錯的機率仍為 **14.33%**。這是由共同失誤機制精確算出的值，不是 LLM 自評信心。它直接说明為何「有分歧才驗證」會漏掉一種重要風險。

這個數字不能外推到真實模型，但它構成有效反例：只要環境容許共同失誤，使用一致性作為唯一停止條件就可能失效。

# 消融、敏感度與排名反轉

## 消融：哪些元件真的有貢獻

@@ABLATION_TABLE@@

去掉 $V$ 或 $E$ 的效果最大，說明主分布的改善主要來自取得正確種類的新資訊，而非模型數量本身。強制用完四次預算雖然提高回答部分的準確率，卻把 $J$ 推高至 **0.5815**；這是「更準確」與「更好的決策服務」不等價的例子。

移除異質工作模型 $C,D$ 的影響較小。更重要的是，假設條件獨立的估計版平均 $J$ 為 **0.3737**，略低於完整聯合估計的 **0.3752**；P1 減此消融組的配對差為 **0.00145**，95% CI **[-0.00084, 0.00373]**，未校正 $p=0.201$。因此不能宣稱相關性建模在本實驗中帶來可靠優勢。可能的原因是估計變異，以及最優行動通常選擇本來就獨立的 $V/E$；這是合理解釋，但本實驗沒有再分離兩者的因果貢獻。

在已知真實分布的同一主設定下，條件獨立近似與完整 P1 恰好得到相同總目標。這不表示獨立假設一般正確，只说明本次最優行動未必需要使用那些有依賴性的訊號。

![消融相對 P1 的配對目標差。誤差棒為診斷性、未作多重校正的 95% 信賴區間。相關性建模消融沒有顯著變差，必須保留此負面發現。](figures/ablation_effects.png){width=85%}

## 凍結策略的分布與成本漂移

策略只以主分布校準，以下環境改變後不重新學習。共同失誤 $G$ 的機率改為 0.32；驗證器準確率改為 0.62；偏好訊號準確率改為 0.55；成本衝擊將 $V$ 成本乘八、$E$ 成本乘四與延遲乘五；簡單任務環境則只含 $A$ 正確率 0.995、無共同翻轉的簡單事實題。每個條件只改動指定部分。

@@STRESS_TABLE@@

這些結果是對 20 組凍結、已估計策略在新分布的**精確期望值取平均**，不是新的抽樣誤差，也不是 API 測量。驗證器退化時，P1 的 $J$ 上升至 **1.3645**，比 C1 的 **0.9539** 更差；當資訊來源本身失真，精密的選擇器也可能有效率地選到錯誤來源。成本衝擊下，P1 仍照舊大量使用原先便宜的工具，因此也輸給 P0。簡單任務環境則讓 P0 的單查 $A$ 成為較佳選擇。

![五種環境改變下的精確期望值。所有政策凍結於主分布；曲線交叉表示排名依賴假設，不能宣稱普遍最佳。](figures/stress_shifts.png){width=85%}

探索性檢查另外允許 P1 得到更新後的真實分布與成本，其最小 $J$ 分別為：共同失誤 0.3893、驗證器退化 0.7024、偏好訊號退化 0.5161、成本衝擊 0.7668、簡單任務 0.1450。這些是**資訊充分時的下界參照**，不是已經部署成功的自動漂移偵測器。它們說明失敗主要揭露了凍結錯誤模型的問題，而不是否定式 (9) 在其前提下的正確性。

## 工具依賴的探索性檢查

主實驗允許 $V$ 直接輸出答案。作為事後新增、明確標記的敏感度檢查，我們改成：**只有在較早一個批次至少取得一個工作模型的答案後，才可呼叫 $V$**。同一批次同時呼叫第一個工作模型與 $V$ 不合法。損失、價格、分布與四次 acquisition 預算保持不變。

@@DEPENDENCY_TABLE@@

此表全部使用已知真實分布的策略與精確期望，不應與主表的估計版數值混比。加入先有候選的限制後，C3 可能看不到「先取得候選，才能解鎖查證」的兩步價值而過早停止；P0 已固定取得候選，因而反過來勝過 C3。P1 能把行動可行性放入式 (9)，在此小型問題仍取得最低值。

這是對「級聯幾乎總是夠好」的必要修正：級聯是否夠好，取決於工具依賴、資訊互補與候選生成的價值。這項探索性實驗沒有模擬候選品質如何改變驗證器錯誤率；它只加入可行性 gate。

![探索性工具依賴分析。是否要求候選先於驗證，足以逆轉 P0 與 C3 的相對排名。](figures/verifier_dependency.png){width=85%}

## 成本與延遲權重

我們在已知分布下對七個 $\lambda$ 與四個 $\mu$ 組合進行精確計算，共 28 個資源權重設定、每組三種策略。不同權重會改變查詢量與平行／序列取捨；沒有單一固定 panel 能保證位於所有權重下的最佳點。

下圖呈現 $\mu=0.04$ 的切片。曲線是不同標量化權重的運作點連線，不宣稱完整枚舉所有非支配解，也不表示固定相同花費下已對任意真實模型做過公平測量。

![成本與終端損失的敏感度。使用已知聯合分布，屬理想化控制問題；點與點之間的線只是視覺連接。](figures/cost_quality.png){width=85%}

# 理論、數值與實作的一致性

## 已知分布最優值與估計版表現

主分布的已知分布 Bellman 最優值為 **0.372812**。估計版 P1 的真實期望值，對 20 次校準重複取平均為 **0.375401**；兩者差 **0.002589**。Monte Carlo 測得的 **0.375175** 則是這些估計版政策在留出樣本上的實現平均。三個數字分別是理論下界、估計政策的真實期望與測試抽樣結果，不能混稱為同一種證據。

高風險事實題的平均 TV 約 **0.04763**，平均 regret 約 **0.01626**，式 (11) 的平均界約 **1.9614**。界確實成立，但非常鬆；它的功能是交代估計誤差如何可能影響最優性，而不是提供實務上精準的信心保證。其他類別的 TV、regret 與上界均在 `estimation_bounds.csv` 完整保留。

## 單元測試與獨立數值證書

修訂後測試套件共 **86 項通過**，其中保留原始 38 項並新增 48 項。檢查包含分布正規化、子歷史機率分割、平行延遲使用最大值而非總和、預算與重複查詢限制、種子重現性、偏好不可辨識、XOR 互補性，以及改變未查詢訊號不影響策略輸出的資訊隔離。

原始遞迴樹評估器會與主求解器共享轉移表，不能當成完全獨立的實作。v1.1 新增不使用 MATCH、TRANS、ADD 或三進位歷史表的 tuple-history 參考求解器，直接由完整結果逐一算條件機率，檢查隨機聯合分布、兩次預算與候選先決條件。這降低共同實作錯誤的風險，但仍不是形式驗證。對十個額外隨機聯合分布的全部 473 個可行歷史，逐一比較終端選項與所有合法批次，驗證 Bellman 最小值條件；主分布也檢查 P1 對所有具名策略的支配關係。這是有限實例的數值證書與程式交叉檢查，不等同於 Lean／Coq 的機器形式證明。相同環境另從頭重跑主實驗；排除執行耗時等 metadata 後，34 個共同結果檔的 SHA-256 全部一致，記錄於 `expected/origin.json` 與 `verification/attestation.json`。這不是外部研究者的獨立重現。

# 討論：對 MoA 設計真正得到的結論

## 不應把原流程改名後宣稱原來就正確

P0 與 P1 不同。P0 是將原提案明確化的固定啟發式流程；P1 是在本次研究中進一步形式化的最佳資訊取得政策。P1 勝出不能用來反推「原先的架構已經被證明最優」。更精確的結論是：原提案包含一些有價值的行動種類，但其順序與觸發邏輯並非最優，甚至可能不如簡單級聯。

## 最有價值的行動未必是再問一個模型

本模擬的主要改善來自四種行為：直接取得有效事實資訊、直接澄清私人偏好、在低利害任務停止，以及在高利害任務保留判斷。這些行為未必需要多個生成模型參與。任何將它們包在「MoA」標籤下的實作，都不應把觀測來源改善誤歸因於多模型智慧。

## Disagreement score 不能直接當作價值函數

高分歧可能來自資訊不足、價值衝突或彼此不相關的雜訊；低分歧可能只是共同失誤。類似「分歧 × 後果 × 可驗證性」的乘積可用作啟發式訊號，卻不是在本研究被證明最優的統計量。真正與目標一致的量，是**下一個合法行動能降低多少預期決策損失，扣除取得資訊的成本與延遲**。

最低 co-failure 也不足以決定 panel。本研究的精確反例中，三個完全相關但正確率 90% 的模型，多數決錯誤率為 10%；三個彼此獨立但各只有 55% 正確率的模型，多數決錯誤率卻為 42.525%。低相關性必須與單體品質共同評估，不能當成唯一指標。

## 決策控制器的建議契約

根據本次模型，合理的控制器輸入應包含：可用證據與其來源、任務利害、使用者已知偏好、合法行動及其依賴、估計條件觀測品質、成本與剩餘預算。輸出應包含選擇的行動、停止或轉人工原因、使用的機率版本及已觀測證據。

在實際系統中，候選文字可由 Hermes 類顧問模式取得，矛盾可由 Fusion 類分析步驟呈現，但這些分析器應是提供資訊的元件，不是自帶最優性的決策權威。驗證器故障與價格改動必須反映在控制器的估計與可行行動中。上述是依據本研究結果提出的工程推論，尚非本次已完成的產品整合。

# 有效性威脅與未解決部分

**建構效度。** 二元訊號不代表完整自然語言回答。模擬沒有生成新解法、工具參數、跨回合記憶、長上下文遺失、辯論說服力或文字融合。本文不能量測這些能力，也不能由此判定商業產品排名。

**資訊來源假設。** 主設定刻意給 $V/E$ 高品質新資訊，同時讓群體偏好與私人偏好獨立，這使查證與偏好澄清具優勢。這是待檢驗的情境假設，不是由實際 LLM 資料估出的事實。驗證器也可能與模型共用錯誤來源；偏好澄清可能有理解錯誤或拒絕回答。

**任務辨識與授權。** 所有策略被告知真實任務類型和錯誤損失，沒有測 router 的分類能力，也沒有測使用者是否授權系統採用特定效用權重。高利害任務的損失值是研究者設定，不能自行當作使用者的價值觀。

**成本與系統效度。** 未計入規劃器、語意分析器與 finalizer 的差異成本。六個通道的完整聯合表在真實高維系統會快速膨脹；有限狀態精確解不保證有可接受的生產延遲。計算環境的實際 CPU 耗時記錄在附件，但不作為模型服務效能結論。

**比較範圍。** P0 只是原文字提案的一個具體版本，不代表每種可能的風險路由策略。C1/C2 沒有新資訊工具，因此其差距部分來自資訊可得性；C3 及消融組用于分離這一點。Fugu 的學習式協調與商業 Fusion 的工具迴圈沒有被重現，不能宣稱打敗它們。

**統計外推。** 信賴區間只涵蓋此合成生成器下的抽樣與校準變異，不能涵蓋生成器與真實世界的落差。大樣本與極小 $p$ 值不會修復這種外部效度限制。探索性依賴測試與主實驗分開報告，避免事後選擇假說。

**最優性邊界。** 證明建立在指定分布、成本與合法行動上，沒有證明未知分布下的最小極大最優、任意自然語言任務的普遍最優、自由文字 Pareto 決策最優，或比所有可能神經協調器更好。

# 真實模型驗證方案（未執行）

若要將本研究升級成「真實 MoA 架構效能論文」，需要另外執行對應的外部效度研究。此節是明確標示的研究設計，不是假裝已完成的實驗。

第一層應使用可程式檢驗的工程任務，讓同一組候選模型與同一種 finalizer 在 C1、C2、C3、P0、P1 下運作；所有組別若允許測試、檢索或查證，必須共享同樣工具與授權。對需要候選才能測試的任務，應將依賴顯式列入策略可行性，而不是讓某組取得無條件真值。

第二層應使用具有明確但部分隱藏使用者偏好的結構化取捨任務，同時測量回答、澄清問題、保留多個方案及轉人工。正確性應相對事前設定或由參與者確認的效用，而不是讓另一個模型代替使用者定義偏好。

第三層才是自由文字開放題：以盲化、隨機順序的獨立人工評審評估事實支持、偏好遵循、未解分歧保留與可行性；將 judge 模型只作為輔助，不讓與策略共享偏誤的單一評審產生主要標籤。真實成本以完整 token、工具費用與牆鐘時間記錄；模型版本、seed、temperature、prompt、截斷、timeout 與錯誤都需要保存。[8]

架構比較應同時包含共同預算上限、相同成本區間及品質－成本前緣；先在校準／開發集調參，再鎖定盲測。低成本啟發式若在盲測內接近較複雜策略，應保留其工程優勢，而不是只用顯著性宣稱較複雜者必要。未取得這些資料前，本研究不能發表任何產品勝率或「市場最佳 MoA」結論。

@@REVISION_SECTION@@

# 結論

本研究沒有證明原先提出的流程最理想，反而找到它的明確反例與實測弱點。它在主模擬中勝過單模型與固定融合，卻輸給較簡單的資訊價值級聯；修正後的 Bellman 控制器有條件式最優保證，但估計版與完整真實系統仍不具有普遍優勢。

重要的設計原則不是「每個問題都讓更多模型參與」，而是「將取得哪一種新資訊、是否付出下一筆成本、何時停止以及誰有權提供偏好，全部納入可稽核的決策問題」。對真正的自行車棚問題，最優行動可能就是不要再開會；對未知偏好，可能是問人；對可查證事實，可能是先查證；對具有互補或依賴的資訊，才需要較深的協調。

最終可支持的主張是：**明確目標、合法行動與經校準資訊模型所導出的策略，能在限定問題中被證明最優；一張更複雜的 MoA 流程圖，不能。**

\clearpage

# 參考文獻 {-}

[1] Junlin Wang, Jue Wang, Ben Athiwaratkun, Ce Zhang, James Zou. *Mixture-of-Agents Enhances Large Language Model Capabilities*. 2024. arXiv:2406.04692. <https://arxiv.org/abs/2406.04692>.

[2] Wenzhe Li, Yong Lin, Mengzhou Xia, Chi Jin. *Rethinking Mixture-of-Agents: Is Mixing Different Large Language Models Beneficial?* 2025. arXiv:2502.00674. <https://arxiv.org/abs/2502.00674>.

[3] Lingjiao Chen, Matei Zaharia, James Zou. *FrugalGPT: How to Use Large Language Models While Reducing Cost and Improving Performance*. 2023. arXiv:2305.05176. <https://arxiv.org/abs/2305.05176>.

[4] Isaac Ong et al. *RouteLLM: Learning to Route LLMs with Preference Data*. 2024. arXiv:2406.18665. <https://arxiv.org/abs/2406.18665>.

[5] OpenRouter. *Fusion Router — openrouter/fusion*. 官方文件，查閱日期 2026-09-17。<https://openrouter.ai/docs/guides/routing/routers/fusion-router>.

[6] Nous Research. *Mixture of Agents | Hermes Agent*. 官方文件，查閱日期 2026-09-17。<https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents/>.

[7] Sakana AI. *Learning to Orchestrate Agents in Natural Language with the Conductor*. 官方研究介紹，2026-04-27；查閱日期 2026-09-17。<https://sakana.ai/learning-to-orchestrate/>.

[8] Lianmin Zheng et al. *Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena*. NeurIPS Datasets and Benchmarks, 2023. arXiv:2306.05685. <https://arxiv.org/abs/2306.05685>.

[9] Yonatan Geifman, Ran El-Yaniv. *SelectiveNet: A Deep Neural Network with an Integrated Reject Option*. ICML, 2019. arXiv:1901.09192. <https://arxiv.org/abs/1901.09192>.

[10] Smitha Milli, Falk Lieder, Thomas L. Griffiths. *When Does Bounded-Optimal Metareasoning Favor Few Cognitive Systems?* AAAI, 2017. DOI:10.1609/aaai.v31i1.11156. <https://ojs.aaai.org/index.php/AAAI/article/view/11156>.

\clearpage

\small

[11] Nicholas Hay, Stuart Russell, David Tolpin, Solomon Eyal Shimony. *Selecting Computations: Theory and Applications*. UAI, 2012. arXiv:1207.5879. <https://arxiv.org/abs/1207.5879>.

[12] Daniel Golovin, Andreas Krause. *Adaptive Submodularity: Theory and Applications in Active Learning and Stochastic Optimization*. JAIR, 2011. arXiv:1003.3967. <https://arxiv.org/abs/1003.3967>.

# 附錄 A：重現指南與資料契約 {-}

## 實際執行環境 {-}

@@ENV_TABLE@@

主程式與後續探索性程式各自保留日志；主協議的 SHA-256 為：

```
3c3cf610fb22bb36aab447f718f6746c5a26b5434d9df795db6507078afe55a8
```

主程式實際完成 @@RUNTIME@@ 秒的 CPU 計算；此為單次本環境耗時，**不是任何 MoA 模型的服務延遲**。論文中表格使用的延遲全部由式 (4) 計算。

## 執行方式 {-}

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python study.py --output results
python -m pytest -q test_study.py test_revision.py test_study.py
OPENBLAS_NUM_THREADS=1 python supplement.py
python make_figures.py
python build_paper.py
```

PDF 排版另需 Pandoc 與 XeLaTeX；`build_paper.py` 先由 CSV 結果生成 Markdown，`build_pdf.sh` 再生成排版稿。字型使用作業系統中的 Noto CJK、Liberation 與 DejaVu 字型，附件不包含字型檔。不同 BLAS／套件版本可能造成極小浮點差異；策略選擇採 $10^{-12}$ 容差處理平手。

## 重要檔案 {-}

@@FILES_TABLE@@

`raw/replicate_XX.npz` 內含 `type`、`y`、`outcome` 三個長度 20,000 的測試陣列，以及該次重複的 `fitted_joint`。`outcome` 是 0–63 的位元編碼，位元順序依序為 A、B、C、D、V、E；策略 rollout 只揭露付費取得的位元。校準資料以平滑後的 joint sufficient statistics 保存，沒有原始文字。

`metrics_by_replicate.csv` 是抽樣測試结果；`exact_expectations.csv` 是同一批已估計策略在真分布下的精確期望。`oracle_known_distribution.csv` 中的 P1 是決策下界，其他行只是使用已知 likelihood 的指定策略，不代表它們也是全域最優。

## 預註冊與探索性分析界線 {-}

`protocol.json` 在主實驗首次執行前保存，包含五組主策略、四項主比较、六項消融、五種漂移及資源掃描範圍；沒有在外部平台註冊。`exploratory_status.json` 明確記錄事後新增的候選－驗證依賴與更新真分布參照。不應在後續投稿時把後兩者改寫為事前假說。

# 附錄 B：偽程式碼與終端決策稽核 {-}

```text
Input: fitted joint distribution, losses, costs, latency, legal actions, budget
For histories in descending number of observed channels:
    posterior <- marginalize the fitted joint over unobserved channels
    stop_value <- min(answer_0_risk, answer_1_risk, defer_cost)
    best <- stop_value
    For each legal unobserved batch S within remaining budget:
        q <- acquisition_cost(S) + batch_latency_penalty(S)
        q <- q + expected optimal value of every possible child history
        If q is smaller: keep S and q
    Store optimal action and value for this history
At runtime:
    Reveal only queried channel outputs
    Follow stored policy until a terminal action
    Score using hidden Y only after termination
```

保存決策 trace 時至少應保留任務類型、估計分布版本、已取得通道、批次邊界、終端動作及所需成本。附帶程式另外測試：若改動的只是終端 trace 沒有讀取的位元，決策、查詢集合、成本及延遲都不得改變。

# 附錄 C：不能從本研究推出的說法 {-}

本研究不支持以下推論：「P1 已擊敗 Fugu／Fusion／Hermes」「400,000 個案例等於 400,000 道真實問答」「數學證明會保證新版本模型的可靠度」「更多模型總是有益」「多样性一律比模型品質重要」「沒有觀測到分歧就可以省略查證」「最低驗證錯誤率代表最低總成本」。

較精確的摘要是：在指定的合成問題中，原啟發式被較简单策略擊敗；在指定的有限行動空間中，已知分布 Bellman 解具最優性；在未知或漂移環境中，估計與行動依賴才是必須繼續驗證的部分。
