# 修訂實驗：分離資訊、適應性與估計誤差

## 修訂範圍與事後性

本節全部是讀取 v1.0 結果後設計的探索性分析，協議保存在 `revision_protocol.json`。沒有外部預註冊，也不能因為使用 Holm 校正就稱為確認性研究。資訊匹配比較沿用主實驗 20 組校準與測試案例，並沒有增加獨立測試題；學習曲線另使用獨立亂數流的 20 次校準重複及精確期望評估。公開程式與 GitHub Actions 能提升計算可重現性，但不會自動提升自然語言外部效度。

## 資訊來源匹配的更強對照

原 C1/C2 只能詢問工作模型，與 P1 的工具權限不同。為避免把新資訊的價值全歸因於規劃，新增四個對照。R0 立即停止；R1 對每類任務在相同估計分布上選擇最好的**一次性批次**，可使用全部六個通道或不查詢，後續不再適應觀測；R2 是只允許 A–D 的單步級聯；R3 是同樣只允許 A–D 的完整前瞻。這形成兩種有效比較：P1 對 R1 比較相同資訊權限下的序列適應性；R3 對 R2 比較僅工作模型環境下的前瞻。

@@MATCHED_ZH@@

P1 對 R1 的配對差為 **−0.00653**，95% CI **[−0.00861, −0.00446]**，相對改善約 **1.71%**。R1 已接近 P1，表示在此生成器下，多輪適應性只提供小額邊際收益。R3 對 R2 則降低 **0.04673**，95% CI **[−0.04905, −0.04441]**。完整前瞻即使不使用 V/E 也可能有價值，但遠不足以重現取得高品質新資訊帶來的改善。

這些是受控政策對比，不是將所有差距精確分解的因果實驗。來源權限、任務型態與停止行為之間存在交互作用，不能把幾個差值直接相加，聲稱某元件貢獻固定百分比。

## 校準樣本數與偏差—變異取捨

每類分別採用 100、500、1,000、5,000、20,000 個校準抽樣；各設定 20 個新的獨立種子。所有策略以同一校準分布建立，測量時對真分布完整積分。合計新增 **2,660,000 次合成校準抽樣**，不是新增真實 LLM 回應或人工標籤。

@@LEARNING_ZH@@

![校準資料量與精確期望損失。每點為 20 個新校準種子的平均；陰影為跨種子的 95% t 區間，並非外部效度區間。](figures/calibration_curve.png){width=92%}

完整聯合估計在小樣本下可過擬合；條件獨立近似雖有結構偏差，部分樣本區間的決策表現反而較好。最低樣本數時也不能聲稱獨立近似一定較佳。C3 的真實損失未必隨樣本數單調下降：因為它只對估計模型作單步最佳化，估計誤差有時可能偶然促使較好的後續行動。本實驗不將這種不單調解讀成資料有害的普遍結論。

## 任務分類錯誤與共同失誤

主研究讓控制器知道真實任務類型與利害，是很強的前提。新增的路由壓力測試讓標籤以機率 $\eta$ 被換成其餘四類中均勻選出的一類，政策不重新校準，評分仍使用實際類型的效用。這同時擾動所選觀測模型與損失權重，**不是純粹的語意分類器準確率實測**。

@@ROUTING_ZH@@

10% 路由錯誤時 P1 的 $J$ 已從 **0.3754** 增為 **0.5376**；這個損害遠大於它相對 C3 的正常環境收益。分類與效用輸入品質不能被架構圖當作免費、完美的能力。

另將事實查證 V 與工作模型的全域錯誤 G 漸進耦合，凍結政策的 P1 損失從 **0.3754** 上升至完全耦合的 **0.8072**。此生成器調整會**同時改變 V 的邊際準確率與依賴結構**，故不能將全部退化解讀為純粹「相關性效應」。它更保守地表示：若來源獨立且可靠的前提不成立，原估計政策就會失準。完整耦合結果在 `results/revision/verifier_coupling.csv`。

## 規劃開銷：理論收益有多薄

新增 CPU 診斷為每種政策連續建立五類完整政策表，獨立量測十次。執行環境與中位數由當次輸出動態記入表格，不將 CPU 秒數混入原先的人工正規化延遲。

@@RUNTIME_ZH@@

政策可以離線編譯並重用，因此不能將整張表建置時間當作每請求線上負擔。反過來，真實文字系統若每題需要額外 judge、狀態抽取或搜尋，這些成本也不能省略。

若只在 P1 加入每題固定的額外目標損失 $h$，不改變其他行為，則它相對 C3 的點估計優勢僅在 $h<0.00727$ 時存在；對 R1 的相應門檻約為 $0.00653$。這些是合成效用單位，**不是可換算的美元或毫秒上限**。當增加維護複雜度前，應先量測真實控制器是否落在可承受區間。

## 數學補強：偏好資訊與近似求解

**命題五（部分偏好資訊的精確界）。** 對公平二元偏好 $Y$，令 $Q_y(o)=P(O=o\mid Y=y)$。只觀測 $O$、必須回答二選一的任意規則，其最高選對機率為

$$
\sup_a P(a(O)=Y)=\frac{1+\operatorname{TV}(Q_0,Q_1)}{2}. \tag{13}
$$

**證明。** 每個 $o$ 選擇聯合機率較大的標籤，即得 $\frac12\sum_o\max\{Q_0(o),Q_1(o)\}$。使用 $\max(x,y)=(x+y+|x-y|)/2$，以及兩分布各自總和為 1，即得式 (13)。任意隨機決策只形成確定規則的混合，不會更好。當 TV 為零時回到命題二；TV 非零時則容許部分可識別性。本式不將轉人工後的結果計作自動選對率。$\square$

**命題六（分布與近似求解誤差）。** 假設某個可行近似策略 $\widetilde\pi$ 在估計模型上，距最佳期望目標不超過 $\delta$；所有可行策略的總損失落在 $[0,M]$，則

$$
J_P(\widetilde\pi)-J_P(\pi_P^*)
\le 2M\operatorname{TV}(P,\widehat P)+\delta. \tag{14}
$$

**證明。** 在左式加減 $J_{\widehat P}(\widetilde\pi)$ 與 $J_{\widehat P}(\pi_P^*)$。兩個跨分布項各由 $M\operatorname{TV}(P,\widehat P)$ 上界；中間項不超過 $\delta$，因為估計模型的最優值不高於 $J_{\widehat P}(\pi_P^*)$。相加即得結論。$\square$

這不是新的通用學習理論；它只是將「世界模型估錯」與「計算沒有求到最優」分開。本文另以隨機有限分布測試數值不等式。若還省略了真實規劃費用或行動依賴，不能只靠式 (14) 得到端到端保證。

## 審稿式自我評估與公開狀態

本研究可支持的是**可重現的合成機制分析與原提案的否證／修正**，不是新一代 LLM 架構的實證突破。Bellman 與 TV 界屬既有決策理論的直接應用；640 個有限支撐點並沒有因抽取 400,000 題就變成多樣的真實工作負載。人工設定高品質 V/E、已知損失與二元訊號，仍強烈影響結論。

新增基線、獨立參考求解器、弱點測試及公開 CI，使稿件更適合作為技術報告或研究工件；但**尚不足以支持強方法論新穎性、SOTA、多產品勝率或頂級會議水準等主張**。自然語言任務、至少一組真實模型／工具、模型版本鎖定、開放偏好的人工效用評估，以及等價工具權限下的端到端成本，仍是後續實證缺口。這份稿件經 AI 協助撰寫、編碼與檢查；沒有虛構外部作者、審稿人或接受紀錄。
