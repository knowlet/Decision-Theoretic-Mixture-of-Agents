---
title: '何時該更新路由器？'
subtitle: '模型池替換下的決策理論、資訊取得，以及型別化信心的界線'
author: 'Decision-Theoretic Mixture-of-Agents 專案'
date: '2026 年 9 月 18 日 · v1.3.0 · 公開技術報告'
lang: zh-TW
---

# 摘要

多模型系統必須分開處理四件事：讓代理學得更好、估計各代理的能力、決定下一次資訊取得是否值得，以及輸出軟體可接受的合法動作。CERA-MoA 同時更新代理與路由器；TypeSafe Jev 則提供型別化機率決策。但兩者都不會自動給出使用者效用，亦不直接證明查詢策略最優。本研究在已發布的有限決策模擬與 RouterBench 歷史重播之外，加入 @@N_SOURCES@@ 份固定版本 ProEval 資料。@@N_PRIMARY@@ 個二元評分任務涵蓋 @@PRIMARY_ROWS@@ 題、八個固定模型身分與 @@PRIMARY_TEST@@ 題留出測試；DICES 另作帶有限制的序位損失診斷。我們比較九種策略，在模型池替換後分別凍結、完整重校準或只取得有限校準樣本。每次查詢成本設為 0.01 時，重校準後 Bellman、靜態批次與貪婪級聯在 @@N_PRIMARY@@ 個二元測試集上的逐題目標值相同，跨資料集平均為 @@REFIT_OBJECTIVE@@。Bellman 重校準相較凍結改善 @@REFIT_GAIN@@，條件式配對 bootstrap 區間為 [@@REFIT_GAIN_LO@@, @@REFIT_GAIN_HI@@]；但小型更新集可能讓結果變差。新增的其中一個任務由保留判斷主導：每個候選的歸檔錯誤率都超過保留判斷損失，因此沒有任何控制器在該任務回傳答案；我們把它留在平均值中並明確說明，而不是事後剔除。來源稽核另揭露數值序列化別名與超出文件範圍的 DICES 標籤。本文給出有限控制、近似誤差與動作間距的條件式保證，明確區分型別安全、校準與決策正確性。完整程式通過 @@TESTS@@ 項測試，並有兩次獨立程序重跑、來源雜湊與逐筆帳本檢查。本研究沒有執行 Jev 推論、CERA-MoA 代理訓練、新 LLM 生成或人類偏好實驗。

# 研究問題與證據層次

原始問題是：當不同模型對開放問題意見分歧時，究竟應依什麼規則決定結果？分歧可能來自事實資訊不足、共同失誤、不同優化方向，也可能只是尚未知道使用者的偏好。多問幾個模型，不會自動取得最後一種資訊。決策系統需要明確的損失、取得資訊的成本、合法動作，以及有權提供偏好的決策者。

本專案 v1.1 使用有限支撐集合成環境；v1.2 使用 RouterBench 真實 LLM 歷史輸出。[5,6] 原本的複雜啟發式在合成資料中輸給較簡單的資訊價值級聯；歷史重播也沒有建立 Bellman 對強靜態／貪婪對照的可靠優勢。這兩版均保留原始公開產物。本版不覆寫那些結果，也不將先前本地分享的傳統分類器代理實驗混入本次資料。

本版聚焦三個較精確的問題：先前結果能否轉移到另一個回答歸檔和新增任務族？模型池變動後，更新能力估計是否比增加單題前瞻深度更有價值？熟悉度分數或型別化信心，需要哪些額外證據，才能連接到決策品質？

本次操作是從既有回答中取得資訊與選擇候選，不會重新生成答案、訓練候選 LLM、執行其推理、融合自由文字，亦不會詢問真人偏好。新增歸檔紀錄提高的是資料覆蓋；它不會自動補足尚未觀測的代理互動機制。

# CERA-MoA 與 Jev：不同層級的貢獻

## CERA-MoA 實際改變了什麼？

CERA-MoA 將路由器更新與代理強化學習結合。[1] 路由器使用凍結主幹模型的中間層查詢特徵；每個代理對應一個可訓練的 predictor head，以及固定隨機 target head。兩者正規化投影的距離形成 $f_i(q)=\exp(-\lambda d_i(q))$ 熟悉度。代理的相對獎勵優勢用來拉近或推遠 predictor 與 anchor，讓能力估計隨代理更新而改變。

訓練時，探索 bonus 影響候選排序，但啟動門檻累加的是原始 familiarity，不是加上 bonus 的值。推論時關閉探索，以達到門檻的最小排序前綴決定啟動代理；若一直無法達標，才啟動全部代理。共享主幹實驗使用獨立 LoRA，論文也測試異質模型池。

這是它比本研究更有實證的部分：作者真的改變了代理參數與樣本分配。本研究的固定歸檔只能測「模型池更換後，路由器是否該重新估計」，不能識別共同訓練帶來的因果效果或專業化。

還有一個與原始自行車棚問題直接相關的細節：確定答案任務使用 familiarity 加權投票；開放題則回傳已啟動代理中 familiarity 最高者的完成結果。附錄 F 明確承認尚未進行跨候選的生成式綜合，也沒有長時間、多回合互動驗證。因此，不能把這個結果當成已解決私人偏好衝突。

論文表 3 報告 adaptive routing 平均生成 367.77 tokens，相較固定 top-2 的 666.37，減少約 44.8%；兩者 ID/OOD 分數分別是 63.2/72.8 與 63.3/72.5。這是針對特定對照的生成 token 帳務，並非所有成本或延遲都下降 45%。表 6 的 router 與 native generation 使用 batch size 1，vLLM generation 則是 batch size 32；不能直接當成同批次的端到端速度比較。公開設定列 seed 42，表格沒有提供多訓練種子的不確定性區間。這些是對主張範圍的限定，不是否認其實測結果。

## Jev 的保證位於哪裡？

TypeSafe 將 Jev 描述為不生成自由字串、平行產生型別化輸出的模型，訓練方法稱為 RLCD。[2] 官方 API 有 Choice、Score 與 Noul。[3] Choice 回傳合法選項、選項機率分布，以及由分布計算的 confidence 統計量。「應選哪個動作」的機率分布，不等於「每個動作會成功」的機率。

官方最大幅度的速度與價格優勢來自供應商自身的 workflow 測試。文章說明參考機率取自兩個 frontier 模型的平均，也承認任務設計與比較設定的限制。符合這種參考，並不等於符合獨立真值或特定使用者偏好。型別合法也不等於內容正確。

我們新增的合約檢查器會依目前狀態重建合法動作集合，禁止選擇尚未取得的候選，檢查機率單純形、confidence 範圍與選項。這是 adapter 合約與授權測試；沒有呼叫 Jev，因此沒有 Jev 分數、校準曲線或實測加速倍數。

## 不應混在一起的比較

| 層級 | CERA-MoA | Jev | 本研究 |
|---|---|---|---|
| 實際介入 | 代理 RL 與 router 共同更新 | 供應商訓練的決策模型 | 離線策略估計與模型池替換 |
| 能力訊號 | Hidden-state familiarity | 型別化機率輸出 | 條件錯誤率與透明 prompt 基線 |
| 控制規則 | Familiarity 前綴門檻 | 呼叫者定義問題與動作 | 成本感知查詢、選擇、保留判斷 |
| 開放式輸出 | 最高熟悉度代理的回答 | 不自由生成字串 | 只選既有回答 |
| 本次證據 | 原論文核對 | 官方資料與合約測試 | 已執行的歷史重播與重校準 |

能力估計器、外部效用、成本感知控制器與授權邊界可以組合，但整合效果仍是待驗證假說。不能把不同系統在不同基準上的分數，排成一個看似公平的排名。

# 決策模型與條件式保證

## 有限資訊取得問題

令 $\theta$ 表示已部署代理群，$h$ 為已取得資訊，$r$ 為剩餘查詢預算。合法動作可以取得尚未使用的批次 $S$，或停止並回傳已取得候選／保留判斷。$P_\theta$ 描述潛在觀測與終端損失的聯合分布。主目標為

$$J_\theta(\pi)=\mathbb E[\ell(a,Y)+cN_{\mathrm{acquired}}],$$

其中 $c=0.01$，錯答損失 1，保留判斷損失 0.25。這些都是研究設定的損失單位，不是美元。令 $R(h)$ 為目前停止時最低的條件預期損失；在已知分布、查詢不改變潛在輸出、成本可加總、行動與預算有限時，

$$V(h,r)=\min\left\{R(h),\min_{S\in\mathcal A(h,r)}\left[c|S|+\mathbb E\{V(h\cup O_S,r-|S|)\mid h\}\right]\right\}.$$

**命題一：有限策略空間中的 Bayes 最優性。** 每步選取上式最小的合法動作，即可達成限定策略空間的最低期望損失。證明以剩餘預算歸納：預算為零時直接比較有限終端選項；預算大於零時，任何策略第一步不是停止，就是某個合法批次。每個可能觀測後，歸納假設給出剩餘問題的最低風險；比較所有可達成的下界即可。隨機化第一步是其值的凸組合，不會比最小值更低。這是標準 Bellman 原理的應用，不是新的通用 MoA 最優定理。

實作以已取得答案之間的相等關係與 INVALID 狀態估計錯誤率。四個來源形成 52 種完整模式、151 種部分狀態，每題最多三次查詢。這是刻意受限的表示法，不保證保留任意語意差異；輸出亦只能選已取得候選，而不能發明新答案。

## 分布誤差、近似策略與安全動作間距

**命題二：模型與求解誤差。** 若任意可行策略總損失落在 $[0,M]$，$\operatorname{TV}(P,\widehat P)\leq\epsilon$，且 $\widehat\pi$ 在估計模型內為 $\delta$-最優，則

$$J_P(\widehat\pi)-J_P(\pi_P^*)\leq 2M\epsilon+\delta.$$

證明是在 regret 中加入再扣除 $J_{\widehat P}(\widehat\pi)$ 與 $J_{\widehat P}(\pi_P^*)$。兩個分布差異項因損失有界，各不超過 $M\epsilon$；中間的求解差異最多 $\delta$。本版二元任務可用 $M=1+3c$ 作保守界，但不能從模型自報 confidence 推得 $\epsilon$。

**命題三：快速近似決策器。** 若替代估計器對每個可達狀態之合法動作最優延續值，誤差一致不超過 $\eta$，且最多還有 $H$ 個決策步，則依近似值貪婪選擇的 regret 不超過 $2H\eta$。單步比較近似最小動作與真正最小動作，最多損失 $2\eta$，再對後續步數歸納即可。若估計的最佳與次佳合法動作差距大於 $2\eta$，則該誤差範圍內不會改變第一步選擇。

這提供「快速路徑加上低間距時升級」的一個條件式依據，但不是 Jev 的保證。全域動作價值誤差界比平均校準、ECE 或 API confidence 更強。只有前提滿足時，才能將 $\delta=2H\eta$ 代回命題二。

## Familiarity 總和與合法動作不是正確性證書

假設兩個模型完全共同失誤，各自正確率均為 0.4。即使各自的機率校準正確，加總為 0.8，也不代表至少一個答對的機率是 0.8；該機率仍為 0.4。因此，尚未證明為正確率的 familiarity 更不能直接加總為聯合成功機率。這不反駁 CERA-MoA 的實驗，而是拒絕把門檻規則誤讀成最低風險證明。

同樣地，`emit_0` 可以完全符合 schema，卻輸出錯誤候選。型別安全限制動作空間，不會改正資料、推理或效用定義。測試套件已執行這兩個反例。

當查詢或訓練動作會改變代理參數時，固定潛在輸出的模型不再涵蓋該介入。需要把群體參數、訓練狀態與行動相依轉移納入問題。本版只在不同評估階段替換模型池，沒有求解完整的共同演化訓練控制。

# 固定協議、資料與對照設計

## 資料來源與分割

ProEval 來源固定在 `9d37b037d1d91f4b78a7ddd74a43a8aecb344c66`，每份檔案另有 SHA-256。[4] 協議雜湊在查看策略成績前提交；當時已看過 schema 與三筆範例，且本研究接續既有 v1.2，因此不是外部預註冊。格式解碼修正與事後數值稽核均另留紀錄。

@@DATASETS@@

GSM8K、SVAMP 加入數值答案任務，StrategyQA 加入是非推理；MMLU 是另一批模型／來源資料，不應算成全新的 benchmark 家族。DICES 不加入二元任務主指標。@@N_SOURCES@@ 個檔案仍來自同一歸檔流程，不能視為 @@N_SOURCES@@ 次獨立收集。GQA 提供看圖後的短答，但歸檔只保存影像編號而非像素，因此本重播只重播答案選擇，純文字 prompt 對照在該任務上處於劣勢。Jigsaw 提供毒性判定，其 gold 是註記者毒性比例以嚴格大於 0.5 門檻化，該規則在可比較配對上完全重現歸檔自身的標籤欄。在宣告損失下，所有決策論控制器都選擇保留的任務為：@@DEFER_DOMINATED@@。這類任務中最佳候選的歸檔錯誤率仍高於 0.25 的保留判斷損失，因此不提供策略區辨力。若屬主要指標任務，我們保留在平均值中並明確說明，而非事後剔除；被隔離的序位診斷同樣如此（@@DEFER_QUARANTINED@@），但它自始就不在二元主指標內。固定的 GQA 版本中有九個預測欄位存放產製過程的 debug 字串而非模型答案，其中六個落在本重播使用的八個模型身分上；這些一律視為無法解析並計入來源稽核。

正規化後完全相同的問題以群組雜湊分派至訓練／開發／測試，比例門檻為 60/20/20，兩組模型共用相同題目身分。MMLU 的 NumPy array 文字表示只解析 literal，不執行建構式。這能排除完全重複問題跨切分，但無法排除近似題、來源偏差或預訓練污染。

原模型槽位是 GPT-4o、Claude 3.5 Haiku、Gemini 2.5 Flash、Gemma 3 12B；替換後是 GPT-5.2、Claude 4.5 Sonnet、Gemini 3 Flash、Gemma 3 27B。這些是歸檔名稱，不是目前 endpoint 規格。身分在查看成績前固定。槽位替換不是同一代理在時間上連續更新的 checkpoint；模型大小、能力和供應商世代同時變動。

## 標籤與公平性

`label_*` 欄位是錯誤，而非答對與否的正向分數。它只用於訓練監督與最終評分，不會進入測試時的取得資訊 callback。數值答案先作精確有理數正規化，是非與選項使用嚴格映射；解析失敗保留為 INVALID。任一槽位標籤非有限或不符合宣告範圍時，兩模型池共同排除該列，並報告排除數。

主要策略共享四個模型與最多三次查詢，沒有任何一組獲得完美驗證器、gold、未查詢答案或私人偏好 oracle。終端決策使用共同的估計錯誤／保留判斷規則。`majority3` 是明確標示的診斷例外，除非全數無法解析，否則直接投票回答。單模型與固定三模型則依定義付出其固定查詢數；其他控制器可以更早停止。

## 九種控制器

比較包含 single、fixed3、static、myopic、bellman、disagreement、majority3、prompt_top1 與 cumulative_score。平滑先驗強度從 1、10、100 在開發集選擇，各策略獨立選參。對未觀測完整模式，使用正的均勻模式先驗與訓練全域錯誤率；這是統計假設，不是語言語意推出的真值先驗。

Prompt 對照多了其他控制器沒有使用的問題文字特徵：2,048 維字元雜湊特徵、只在訓練資料擬合的 TF-IDF，以及四輸出 ridge 錯誤估計器。Cumulative 門檻從 0.7、1.4、2.1 在開發集選擇。它們共用主要策略的終端選擇，主要比較資訊取得方式；不應假裝與只看答案相等模式的策略擁有完全相同的特徵。

這個 cumulative 對照不是 CERA-MoA 重現：沒有 hidden states、random target heads、relative advantage、代理 RL、LoRA 或 CERA 加權投票，亦使用共同三次查詢上限，而非在門檻未達成時呼叫全部四個模型。它也不是 Jev 的廉價替代品測試。

## 替換、更新成本與統計

表格中 L 為原模型池；F 為原策略及終端估計直接套用於新模型；R 為使用新模型的訓練／開發資料重新擬合與選參，測試題身分不變。Refresh-50/200 僅取固定排序的非測試訓練前綴，沿用原模型池選出的超參數，不使用新模型的開發資料選參。每個更新題目需四份模型回答，這筆成本另行記錄。這是 full-panel feedback，不是只看到已選動作回饋的 bandit 學習。

三個事前指定對比在各二元資料集內對問題群組重抽 2,000 次，再以四資料集等權平均。資料集使用由同一 root seed 派生的獨立亂數流；區間條件於已擬合策略。重複階段、重跑程序或重複題目不當成新的獨立樣本。十等寬 bin 的 ECE 與 Brier 只評估已回答二元案例的終端錯誤預測，不是 CERA familiarity 或 Jev 校準；DICES 不計算這兩項。

# 實際結果

## 主要收益來自重校準，而非更深前瞻

@@MACRO@@

重校準後，Bellman、static 與 myopic 在全部配對二元測試案例上的目標值相同，跨資料集平均 $J=@@REFIT_OBJECTIVE@@$。這不是「未達顯著，所以視為相同」，而是已觀察逐題數值相同；但仍不能證明母體等價。即使候選選擇與查詢數相同，批次排程仍可能不同。各任務的查詢數見下表。前四個已發表任務的切分 salt 與歸檔答案未變，其各項逐任務結果都與前版相符，僅其中一個 Brier 分數有浮點加總層級的差異；新增的 GQA 則每個控制器都選擇保留；其中單模型控制仍先支付一次查詢，之後才保留，因為在宣告損失下保留判斷才是最佳行動。

@@TASKS@@

@@PAIRS@@

Bellman 從凍結轉為重校準的宏平均改善是 @@REFIT_GAIN@@，其條件式區間沒有跨零。收益包含更新能力估計、更換候選、查詢與保留判斷行為，不能歸因於共同訓練代理。靜態與貪婪對照也得到相同重校準結果，更不能因此宣稱必須使用完整 Bellman 控制。

Cumulative 與 prompt 對照的分數不代表 CERA 或 Jev 輸給本方法；我們沒有執行那兩套模型或訓練流程。不同資訊來源與方法定義，必須保留在解讀中。

## 更新資料不是免費，也可能造成退步

@@REFRESH@@

若更新成本為 $K$，未來分布穩定、每題預期損失改善為 $\Delta>0$，以樣本估計的損益兩平題數為 $\lceil K/\Delta\rceil$。這不是保證，也不是實際美元報酬；收益非正時，不宣稱存在有限回收期。完整訓練與開發資料取得成本也保存在 `calibration_resources.csv`。

在本次切分中，小型 refresh 讓 MMLU 與 SVAMP 的 Bellman 表現變差，樣本從 50 增加到 200 也不保證測試結果單調改善。因此資料支持的是「檢查更新後的策略」，不是每次取得新回饋就盲目替換舊策略。這些結果不驗證選擇性回饋偏差，也不驗證 LLM 持續訓練。

## 校準品質必須另外衡量

@@CALIBRATION@@

降低決策損失，不要求每個有限樣本的 ECE 都同步下降。這些指標只包含已回答案例，因此比較時還要看 coverage。它們使用歸檔二元標籤和本控制器終端風險預測；Jev 的選項機率與 confidence 並未接受測量，DICES 的序位誤差也不能轉成答對機率。

# 來源品質與有效性限制

Gemma 3 12B 在 SVAMP 有 104 組精確答案／gold 比較與歸檔標籤不同。事後加入相對容差 $10^{-9}$、絕對容差 $10^{-12}$ 的稽核，可解釋其中 103 組，符合整數被序列化為帶機器精度殘差的小數的情況。我們沒有因此回頭更改主實驗觀測、標籤、選參或結果。所有模型、GSM8K 的殘餘差異亦保留在稽核表。這同時揭露：以精確數值相等作為狀態，會受到序列化方式影響；稽核不是為了挑出較漂亮結果而偷偷重跑。

DICES 的限制更大。1,500 列中有 439 列不符合所選模型共同的文件範圍篩選條件；留下 1,061 列，卻只有 308 個完全不同的題目群組。來源文件把誤差描述為 $[0,1]$，實際資料卻可見 1.25，甚至一筆 5.5。這可能涉及資料處理或度量定義，但沒有更多證據前不猜測原因。依主設定保留判斷損失，保留子集的 Bayes 控制器全部停止並轉交。這不是有用的人類偏好決策成果；DICES 被排除於二元主結論之外，不能代替特定個人的偏好或代表性安全判斷。

本版嚴格稽核使用相同歸檔中的 reference answer；不同於 v1.2 另外連接 MMLU 參考來源，這次不能稱為獨立原始 benchmark 真值驗證。完全相同問題的群組隔離，亦不能排除近似題、模型訓練污染與來源選樣偏差。永久研究包不再散布原始問題、回答與 reasoning。

額外成本網格是診斷分析，不會拿其中有利的價格替換主設定。第二次程序重跑提高計算可重現性，並不會消除來源與部署場景的落差。全部策略都沒有實測服務延遲；CPU 時間只包含 Python 重播與帳本開銷。

# 執行、驗證與公開產物

實際驗證來源 commit 為 `@@COMMIT@@`，執行紀錄為 @@RUN_URL@@。共有 **@@TESTS@@ 項測試通過，零失敗、零跳過**。兩個完整程序均重做訓練、開發選參、所有階段與資料稽核。**@@OUTPUTS@@ 個非計時結果檔**通過比較規則，其中 **@@IDENTICAL@@ 個逐位元相同**；另兩個帶計時欄位的檔案，只排除耗時欄位再比對。

Gate 會從逐題帳本重新計算損失與查詢成本，要求配對策略的題目集合相同，拒絕選擇未取得候選，檢查預算、切分隔離、固定協議與來源 SHA-256。另有直接過濾完整支撐集的遞迴求解器，不共用正式程式的轉移表。反例測試驗證「熟悉度和不是聯合成功率」及「合法輸出仍可答錯」。Jev 相容合約測試只驗證格式與授權，不代表實際服務已被測試。

計算工作只有 repository 讀取權限；發布工作需先通過證據與檔案校驗，才使用寫入權限，而且不覆寫舊版本。研究包包含程式、衍生軌跡、逐題帳本、測試、日誌、來源資訊與中英文論文，但不包含原始 CSV、reasoning 或字型檔。公開技術報告不是同儕審查、會議接受、arXiv 上架或 DOI 登記。

# 討論與結論

CERA-MoA 提供代理與路由共同學習的實證，這不是本次歷史重播能驗證的介入。Jev 提供面向軟體的型別化決策界面，也不等於知道下一次查詢值不值得。兩者反而讓研究問題更清楚：能力估計、動作合法性、統計校準與外部效用，是四個不同要求。

本版支持的結論比「提出最優架構」更狹窄，也更可操作：路由器能力估計過時時，重校準可能有價值；在同一更新模型裡，完整前瞻未必比強靜態或貪婪策略多帶來收益。校準樣本不足與來源品質問題，也可能抹掉這些好處。未來結合校準、更新成本、合法動作與人類效用的控制系統，是值得驗證的假說，而不是把原始啟發式重新命名後宣稱它已經正確。

尚未完成的實驗包括：真正 CERA 代理訓練、相同任務上 Jev 的即時呼叫、行動相依多回合生成、真實驗證器品質、完整 token／工具／延遲成本、選擇性回饋偏差、人類效用，以及獨立收集的未知任務。共同演化研究應以因子設計分開代理更新、路由器更新與樣本分配，且匹配訓練與服務預算。Jev 實驗則應固定相同授權狀態與動作合約，以獨立標籤衡量語意結果、校準、coverage 與端到端延遲。這些是後續研究設計，沒有被表述成已執行。

# 參考文獻、揭露與資料歸屬

[1] Jiaxuan Jiang、Liyuan He、Zhixuan Fang。*CERA-MoA: Co-Evolving Routing Mechanisms with Continually Learning LLM Agents*。arXiv:2609.18779v1，2026。https://arxiv.org/abs/2609.18779。

[2] TypeSafe AI。*Introducing System One Models & Jev*。官方發布文章，2026-09-18 查閱。https://typesafe.ai/blog/introducing-system-one-models-and-jev。

[3] TypeSafe AI。*System One API and Confidence*。官方文件，2026-09-18 查閱。https://docs.typesafe.ai/ 與 https://docs.typesafe.ai/confidence。

[4] Google DeepMind。*ProEval 資料歸檔與說明*。固定 commit `9d37b037d1d91f4b78a7ddd74a43a8aecb344c66`。https://github.com/google-deepmind/proeval/tree/9d37b037d1d91f4b78a7ddd74a43a8aecb344c66/data。文件標示 Huang、Zeng、Kumaresan、Wang 及各原始 benchmark 作者；材料採 CC-BY 4.0，另有來源別例外。本研究計算衍生紀錄、揭露表示法修改，不主張擁有原始資料或模型輸出。

[5] Decision-Theoretic Mixture-of-Agents 專案。*When to Ask Another Model*，v1.1.0 合成技術報告，2026。https://github.com/knowlet/Decision-Theoretic-Mixture-of-Agents/releases/tag/v1.1.0。

[6] 同專案。*Verified Historical LLM Replay*，v1.2.0，2026。https://github.com/knowlet/Decision-Theoretic-Mixture-of-Agents/releases/tag/v1.2.0。

程式、寫作與檢查使用 AI 協助。不暗示外部審查、機構背書、新模型訓練或論文接受。專案所有者尚未指定全面重用授權；第三方材料維持其原始權利與條件。
